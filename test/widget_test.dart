import 'package:flutter_test/flutter_test.dart';

import 'package:face_imv/main.dart';

void main() {
  test('FaceAnalyzerApp can be constructed', () {
    expect(const FaceAnalyzerApp(), isNotNull);
  });
}
